import { ClassName } from "../utils/types/constantTypes";

const dropDown:ClassName = {
    bg_dark: 'watch-drop absolute top-60 right-0 bg-slate-900',
    bg_light: 'watch-drop is-light absolute top-60 right-0 bg-light_inner'
};

export default dropDown;
